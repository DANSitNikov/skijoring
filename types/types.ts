export type Dog = {
  id: string;
  name: string;
  userId: string;
  createdAt: Date;
};

export type Event = {
  id: string;
  title: string;
  description: string;
  startDate: Date;
  endDate: Date;
  userId: string;
};
