import React, { memo } from "react";

const Footer = () => {
  return (
    <div className="w-full p-4 absolute bottom-0 border-t-2 border-t-black">
      footer
    </div>
  );
};

export default memo(Footer);
